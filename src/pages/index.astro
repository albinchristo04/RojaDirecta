---
import Layout from '../layouts/Layout.astro';
import MatchCard from '../components/MatchCard.astro';
import { getMatches, groupMatchesByLeague } from '../utils/data';

const matches = await getMatches();
const groupedMatches = groupMatchesByLeague(matches);

const title = "Roja Directa En Vivo - Ver Fútbol Gratis Hoy Online";
const description = "Disfruta de todos los partidos de fútbol en vivo hoy. Roja Directa, Tarjeta Roja, Pirlo TV y más canales para ver fútbol gratis online sin registro.";
---

<Layout title={title} description={description}>
	<section class="hero">
		<div class="container">
			<h1>Fútbol en Vivo Hoy</h1>
			<p>Sigue tus equipos favoritos con la mejor calidad de streaming gratis.</p>
		</div>
	</section>

	<section class="matches-section">
		<div class="container">
			{Object.entries(groupedMatches).map(([league, leagueMatches]) => (
				<div class="league-group">
					<h2 class="league-title">{league}</h2>
					<div class="matches-grid">
						{leagueMatches.map((match) => (
							<MatchCard match={match} />
						))}
					</div>
				</div>
			))}
		</div>
	</section>

	<section class="seo-content container">
		<div class="glass content-box">
			<h2>¿Dónde ver fútbol en vivo hoy gratis?</h2>
			<p>
				En <strong>Roja Directa En Vivo</strong> te ofrecemos la mejor recopilación de enlaces para ver fútbol online. 
				Desde la <strong>Champions League</strong> hasta <strong>LaLiga</strong>, pasando por la Premier League y torneos sudamericanos.
			</p>
			<h3>Rojadirecta y Tarjeta Roja</h3>
			<p>
				Somos el portal de referencia para los aficionados al deporte rey. Aquí encontrarás canales como ESPN, Fox Sports, 
				Movistar Deportes y many más, totalmente gratis y en alta definición.
			</p>
		</div>
	</section>
</Layout>

<style>
	.hero {
		padding: 4rem 0;
		text-align: center;
		background: linear-gradient(180deg, rgba(255, 62, 62, 0.1) 0%, rgba(15, 17, 21, 0) 100%);
	}

	.hero h1 {
		font-size: 3.5rem;
		font-weight: 800;
		margin-bottom: 1rem;
		letter-spacing: -1px;
	}

	.hero p {
		font-size: 1.25rem;
		color: var(--text-muted);
		max-width: 600px;
		margin: 0 auto;
	}

	.matches-section {
		padding: 2rem 0;
	}

	.league-title {
		font-size: 1.5rem;
		font-weight: 700;
		margin: 2rem 0 1rem;
		padding-left: 1rem;
		border-left: 4px solid var(--primary);
	}

	.matches-grid {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
		gap: 1.5rem;
	}

	.seo-content {
		padding: 4rem 0;
	}

	.content-box {
		padding: 3rem;
		border-radius: 24px;
	}

	.content-box h2 {
		margin-bottom: 1.5rem;
	}

	.content-box h3 {
		margin: 2rem 0 1rem;
	}

	.content-box p {
		margin-bottom: 1rem;
		color: var(--text-muted);
	}

	@media (max-width: 768px) {
		.hero h1 {
			font-size: 2.5rem;
		}
		.matches-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
