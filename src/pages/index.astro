---
import Layout from '../layouts/Layout.astro';
import MatchCard from '../components/MatchCard.astro';
import { getMatches } from '../utils/data';

const matches = await getMatches();

const title = "Roja Directa En Vivo - Ver Fútbol Gratis Hoy Online";
const description = "Disfruta de todos los partidos de fútbol en vivo hoy. Roja Directa, Tarjeta Roja, Pirlo TV y más canales para ver fútbol gratis online sin registro.";
---

<Layout title={title} description={description}>
	<div class="container">
        <div class="schedule-box">
            <div class="schedule-header">
                <span class="globe-icon">🌍</span>
                TODAY'S LIVE SCHEDULE
            </div>
            <div class="matches-list">
                {matches.length > 0 ? (
                    matches.map((match) => (
                        <MatchCard match={match} />
                    ))
                ) : (
                    <div class="no-matches">No hay partidos programados en este momento.</div>
                )}
            </div>
        </div>

        <section class="seo-content">
            <div class="content-box">
                <h2>Watch Live Football on Tarjeta Roja - The Best Alternative</h2>
                <p>
                    Welcome to Roja Directa En Vivo, the leading portal for enjoying the best sporting events completely free. 
                    If you are looking for a reliable alternative to Tarjeta Roja or Pirlo TV, you are in the right place.
                </p>
                <p>
                    We offer high-quality streams for all major leagues including LaLiga, Premier League, Champions League, 
                    and more. Our links are updated in real-time to ensure you never miss a goal.
                </p>
                <div style="font-size: 0.7rem; color: #eee; margin-top: 2rem;">Verification Code: 5732095</div>
            </div>
        </section>
    </div>
</Layout>

<style>
    .schedule-box {
        background: #222;
        border-radius: 8px;
        overflow: hidden;
        margin-top: 2rem;
        border: 1px solid #333;
    }

    .schedule-header {
        background: #1a1a1a;
        color: #ffc107;
        padding: 1rem;
        text-align: center;
        font-weight: 800;
        font-size: 1.1rem;
        border-bottom: 2px solid #d32f2f;
    }

    .globe-icon {
        margin-right: 0.5rem;
    }

    .matches-list {
        background: white;
    }

    .no-matches {
        padding: 2rem;
        text-align: center;
        color: #666;
        background: white;
    }

    .seo-content {
        margin-top: 3rem;
        background: white;
        color: #333;
        padding: 2rem;
        border-radius: 8px;
        border-left: 5px solid #d32f2f;
    }

    .seo-content h2 {
        color: #d32f2f;
        margin-bottom: 1rem;
        font-size: 1.5rem;
    }

    .seo-content p {
        margin-bottom: 1rem;
        line-height: 1.8;
    }
</style>
